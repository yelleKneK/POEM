# Builds the pkgdown site and publishes it to the gh-pages branch, which
# GitHub Pages serves at the URL named in _pkgdown.yml and in DESCRIPTION.
# Derived from https://github.com/r-lib/actions/tree/v2/examples
#
# Publishing this site is what keeps CRAN's incoming URL check from
# raising "invalid URL ... Status: 404" against
# https://yelleknek.github.io/POEM/, which DESCRIPTION advertises.
#
# One manual step is needed the first time: after this workflow runs and
# creates the gh-pages branch, set Settings -> Pages -> Source to "Deploy
# from a branch", branch "gh-pages", folder "/ (root)".
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  release:
    types: [published]
  workflow_dispatch:

name: pkgdown.yaml

permissions: read-all

jobs:
  pkgdown:
    runs-on: ubuntu-latest
    # Only restrict concurrency for non-PR jobs
    concurrency:
      group: pkgdown-${{ github.event_name != 'pull_request' || github.run_id }}
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: r-lib/actions/setup-pandoc@v2

      - uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true

      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: any::pkgdown, local::.
          needs: website

      - name: Build site
        run: pkgdown::build_site_github_pages(new_process = FALSE, install = FALSE)
        shell: Rscript {0}

      - name: Deploy to GitHub pages
        if: github.event_name != 'pull_request'
        uses: JamesIves/github-pages-deploy-action@v4.5.0
        with:
          clean: false
          branch: gh-pages
          folder: docs
