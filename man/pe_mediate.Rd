% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pe_mediate.R
\name{pe_mediate}
\alias{pe_mediate}
\title{Power-enhanced mediation test from a data frame (formula interface)}
\usage{
pe_mediate(
  formula,
  data,
  mediators,
  confounders = NULL,
  outcome = c("continuous", "binary", "count"),
  ...
)
}
\arguments{
\item{formula}{A two-sided formula \code{outcome ~ exposure} naming the
outcome and one or more exposure columns in \code{data}.}

\item{data}{A \code{data.frame} containing the outcome, exposure, mediator,
and any confounder columns.}

\item{mediators}{Character vector of mediator column names in \code{data}
(the candidate mediators \code{M}).}

\item{confounders}{Optional character vector of confounder column names.
Factor and character columns are expanded into indicator variables;
numeric columns enter as is. Default \code{NULL}.}

\item{outcome}{Outcome type: \code{"continuous"}, \code{"binary"}, or \code{"count"}.}

\item{...}{Further arguments passed to \code{\link[=pe_mediation]{pe_mediation()}} (for example
\code{method}, \code{error_level}, \code{conf_level}, \code{report_all_methods},
\code{lambda_grid}).}
}
\value{
The tidy \code{poem_tbl} returned by \code{\link[=pe_mediation]{pe_mediation()}}.
}
\description{
A convenience front end to \code{\link[=pe_mediation]{pe_mediation()}} for users who have a data
frame rather than ready-made matrices. Give it the outcome and exposure
as a formula, name the mediator columns, and (optionally) name
confounder columns; it assembles the exposure, outcome, mediator, and
confounder matrices (turning factor confounders into indicator
variables) and runs the test. This is the gentler entry point; the
matrix interface \code{\link[=pe_mediation]{pe_mediation()}} is the workhorse.
}
\examples{
# Build a small data frame and test through the formula interface.
set.seed(113)
d <- simulate_mediation_data(n = 200, p = 60, outcome = "continuous",
                             pattern = "contrasting", c1 = 1)
df <- data.frame(y = d$Y, x = d$X[, 1], d$M)
meds <- grep("^X", names(df), value = TRUE)   # the mediator columns
# The mediator columns are named, so the footer names the active ones.
pe_mediate(y ~ x, data = df, mediators = meds, outcome = "continuous")

}
\seealso{
\code{\link[=pe_mediation]{pe_mediation()}} for the matrix interface,
\code{\link[=WHO_mediation_design]{WHO_mediation_design()}} for the worked health-expenditure design.

Other mediation tests:
\code{\link[=pe_lambda_grid]{pe_lambda_grid()}},
\code{\link[=pe_mediation]{pe_mediation()}},
\code{\link[=pe_mediation_linear]{pe_mediation_linear()}},
\code{\link[=pe_mediation_logistic]{pe_mediation_logistic()}},
\code{\link[=pe_mediation_poisson]{pe_mediation_poisson()}},
\code{\link[=pe_mediators]{pe_mediators()}},
\code{\link[=pe_selection]{pe_selection()}},
\code{\link[=summary.poem_tbl]{summary.poem_tbl()}}
}
\author{
Xiufan Yu and Ken Kelley
}
\concept{mediation tests}
