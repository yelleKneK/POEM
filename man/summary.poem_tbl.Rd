% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary_poem_tbl.R
\name{summary.poem_tbl}
\alias{summary.poem_tbl}
\title{Summarize a POEM table}
\usage{
\method{summary}{poem_tbl}(object, alpha_level = 0.05, ...)
}
\arguments{
\item{object}{A \code{poem_tbl}.}

\item{alpha_level}{Significance level for counting a group as a detection.
Default 0.05.}

\item{...}{Ignored.}
}
\value{
For a grouped comparison table, an object of class
\code{summary.poem_tbl} (a list, printed by its own method) with elements
\code{outcome}, \code{alpha_level}, \code{n_groups}, \code{n_with_data}, \code{n_hdmm}, \code{n_pe} (groups
detected by each test), \code{pe_only} (a \code{data.frame} of the groups the PE
test detects but the benchmark does not), and \code{top_mediators} (a
frequency table of flagged mediators). For any other \code{poem_tbl}, the
value of \code{\link[=summary.data.frame]{summary.data.frame()}}.
}
\description{
For a grouped table that compares the benchmark and power-enhanced tests
(any \code{poem_tbl} with a \code{group} column, a \code{pval_hdmm} column, and a
power-enhanced p-value column (\code{pval_pe} or \code{pval_pe_bonferroni}), such as
the output of \code{\link[=WHO_mediation_analysis]{WHO_mediation_analysis()}}), \code{summary()} digests it across
groups: how many groups each test detects mediation in, and in particular
the groups where the power-enhanced test detects mediation that the
benchmark misses, together with the most frequently flagged mediators. For
any other \code{poem_tbl} it falls back to the ordinary data-frame summary.
}
\examples{
# The global and income-group infant-mortality models on a 25-point
# tuning grid, which keeps the example fast; the article's tables use
# the default 100-point grid.
res <- WHO_mediation_analysis("imr", groupings = c("global", "income"),
                              lambda_grid = seq(0.1, 10, length.out = 25))
summary(res)

}
\seealso{
\code{\link[=WHO_mediation_analysis]{WHO_mediation_analysis()}}, \code{\link[=pe_mediation]{pe_mediation()}}.

Other mediation tests:
\code{\link[=pe_lambda_grid]{pe_lambda_grid()}},
\code{\link[=pe_mediate]{pe_mediate()}},
\code{\link[=pe_mediation]{pe_mediation()}},
\code{\link[=pe_mediation_linear]{pe_mediation_linear()}},
\code{\link[=pe_mediation_logistic]{pe_mediation_logistic()}},
\code{\link[=pe_mediation_poisson]{pe_mediation_poisson()}},
\code{\link[=pe_mediators]{pe_mediators()}},
\code{\link[=pe_selection]{pe_selection()}}
}
\author{
Xiufan Yu and Ken Kelley
}
\concept{mediation tests}
