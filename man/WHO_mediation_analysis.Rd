% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/WHO_mediation_analysis.R
\name{WHO_mediation_analysis}
\alias{WHO_mediation_analysis}
\title{Reproduce the article's empirical mediation analysis}
\usage{
WHO_mediation_analysis(
  outcome = c("imr", "u5mr", "leb", "lbw", "pou"),
  groupings = c("global", "region", "income"),
  error_level = 0.05,
  lambda_grid = seq(0.1, 10, length.out = 100),
  lambda_grid_global = seq(0.1, 5, length.out = 100),
  data = WHO_health_mediation,
  cores = 1L,
  verbose = FALSE,
  full_table = FALSE
)
}
\arguments{
\item{outcome}{Which health outcome to analyze: \code{"imr"}, \code{"u5mr"},
\code{"leb"}, \code{"lbw"}, or \code{"pou"}. See \link{WHO_health_mediation}.}

\item{groupings}{Character vector of groupings to run, any of
\code{"global"}, \code{"region"}, \code{"income"}, and \code{"region_income"}. Default
\code{c("global", "region", "income")} (the region-by-income cells are the
slowest and are opt-in).}

\item{error_level}{Target familywise error rate for the
mediator-screening step. Default 0.05, as in Yu and Kelley (in press).}

\item{lambda_grid}{Tuning-parameter grid for the subgroup penalized fits
(regional, income, and region-by-income models). Default
\code{seq(0.1, 10, length.out = 100)}, as in Yu and Kelley (in press).}

\item{lambda_grid_global}{Tuning-parameter grid for the global (all-country)
model only. Default \code{seq(0.1, 5, length.out = 100)}, the narrower
range Yu and Kelley (in press) use for the global fit.}

\item{data}{The source data. Defaults to \link{WHO_health_mediation}.}

\item{cores}{Number of CPU cores; values above 1 fit the subgroups in
parallel via the base \pkg{parallel} package (Unix only). Default 1.}

\item{verbose}{Logical; if \code{TRUE}, print each group as it is fit.
Default \code{FALSE}.}

\item{full_table}{Logical; if \code{TRUE}, return the article's
\emph{extended} table layout, with the power-enhanced p-value and the
active mediator codes reported separately under each multiplicity
method (Bonferroni for FWER, BH and BY for FDR) instead of a single
primary method. Default \code{FALSE}.}
}
\value{
A \code{data.frame} with one row per fitted subgroup. With
\code{full_table = FALSE} (the default) the columns are \code{grouping}
(global / region / income / region_income), \code{group} (the specific
group, for example \code{AFR} or \code{Low}), \code{n_countries} (number of countries),
\code{pval_hdmm} and \code{pval_pe} (the benchmark and power-enhanced p-values
for the total indirect effect), \code{n_active} (number of active mediators
identified), and \code{active_mediators} (their codes, or \code{"none"}). With
\code{full_table = TRUE} the single \code{pval_pe}/\code{n_active}/\code{active_mediators}
columns are replaced by \code{pval_pe_bonferroni}, \code{pval_pe_bh},
\code{pval_pe_by} and the matching \code{active_bonferroni}, \code{active_bh},
\code{active_by} code columns. The result is a \code{poem_tbl} with the
attributes \code{"outcome"}, \code{"full_table"}, and \code{"notes"} (see Details);
call \link[=summary.poem_tbl]{summary()} on it for a cross-group digest of
where the power-enhanced test detects mediation the benchmark misses.
}
\description{
Runs the article's real-data analysis for one health outcome across the
groupings it reports: a global model using all WHO members, then
separate models within each WHO region, within each World Bank income
group, and (optionally) within each region-by-income cell. For every
grouping it reports the benchmark Wald test and the power-enhanced test
on the total indirect effect of health expenditure, together with the
individual mediators the PE test identifies as active. This reproduces
the structure of the article's data-analysis
tables (for example its Table for infant mortality) from the shipped
\link{WHO_health_mediation} data.
}
\details{
The output is the package's \strong{benchmark fit}: applied to the
benchmark \link{WHO_health_mediation} data with the default settings, it
returns the active mediators reported in the article, and the
package's test suite checks those values, so the fit
serves as a fixed reference point for the method.

Each subgroup is fit with the article's preprocessing: the exposure and
the 57 mediators are standardized, the outcome is centered, and the
confounder indicators (region, income, year) are left unscaled. Groups
with too few complete observations (some region-by-income cells are
empty or nearly so) are skipped and reported as \code{NA} rows, named in a
message; a group whose fit fails is likewise reported as \code{NA}, with a
warning that quotes the error. Both are recorded in the \code{"notes"}
attribute (a \code{data.frame} with \code{group}, \code{stage}, and \code{message}, or
\code{NULL} when every group was fit). Because every cell fits a penalized
model over a grid of tuning parameters, a full run with all groupings
fits dozens of models and takes about ten seconds on a current laptop; start with the
default groupings, or a single outcome, before scaling up.

Exact p-values depend on modeling choices that the article fix
(the tuning-parameter grid, the unscaled confounders); the active
mediators the PE test identifies are the stable, substantive output and
reproduce the article's findings (for example, general government
expenditure as a percent of GDP, \code{gge_gdp}, for the global
infant-mortality model).
}
\examples{
# The global infant-mortality model, the article's headline row (under a
# second). Add "region" and "income" to groupings for the full table.
WHO_mediation_analysis("imr", groupings = "global")

}
\references{
Yu, X., and Kelley, K. (in press). Power Enhancement in
High-Dimensional Heterogeneous Mediation Analysis. \emph{Journal of the
American Statistical Association}.
}
\seealso{
\code{\link[=WHO_mediation_design]{WHO_mediation_design()}} for a single design,
\link{WHO_health_mediation} for the data, \code{\link[=pe_mediation]{pe_mediation()}} for the test.

Other WHO mediation data:
\code{\link{WHO_health_mediation}},
\code{\link{WHO_indicator_codebook}},
\code{\link[=WHO_mediation_design]{WHO_mediation_design()}}
}
\author{
Xiufan Yu and Ken Kelley
}
\concept{WHO mediation data}
