% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ss_power_pe_mediation.R
\name{ss_power_pe_mediation}
\alias{ss_power_pe_mediation}
\title{Simulation-based sample size planning for the power-enhanced mediation test}
\usage{
ss_power_pe_mediation(
  outcome = c("continuous", "binary", "count"),
  pattern = c("homogeneous", "contrasting", "heterogeneous"),
  p,
  n_grid,
  c1 = 1,
  c2 = 0.5,
  target_power = 0.8,
  n_rep = 200,
  alpha_level = 0.05,
  method = c("Bonferroni", "BH", "BY"),
  error_level = 0.05,
  lambda_grid = NULL,
  lambda_grid_reduced = NULL,
  cores = 1L,
  seed = NULL
)
}
\arguments{
\item{outcome}{Outcome type: \code{"continuous"}, \code{"binary"}, or \code{"count"}.}

\item{pattern}{Mediation pattern: \code{"homogeneous"} or \code{"contrasting"}
(\code{"heterogeneous"} is a synonym for \code{"contrasting"}).}

\item{p}{Number of candidate mediators.}

\item{n_grid}{Vector of candidate sample sizes to evaluate.}

\item{c1}{Signal strength (the exposure-on-mediator scale). Default 1.}

\item{c2}{Direct effect. Default 0.5.}

\item{target_power}{Desired power for the PE test. Default 0.8.}

\item{n_rep}{Monte Carlo replications per candidate \code{n}. Default 200.}

\item{alpha_level}{Significance level. Default 0.05.}

\item{method, error_level}{Passed to \code{\link[=pe_mediation]{pe_mediation()}}.}

\item{lambda_grid, lambda_grid_reduced}{Passed to \code{\link[=pe_mediation]{pe_mediation()}}; the
default \code{NULL} uses the family's default grid at each candidate \code{n}
(for a continuous outcome the article's grid rescaled by
\code{\link[=pe_lambda_grid]{pe_lambda_grid()}}, so the grid follows the sample size being planned).}

\item{cores}{Number of CPU cores (Unix forking). A seeded parallel run is
reproducible across runs at the same \code{cores} but need not match a
serial run. Default 1.}

\item{seed}{Optional integer seed, set locally and restored on exit.}
}
\value{
A tidy \code{data.frame} (class \code{poem_tbl}) with one row per
candidate sample size and columns \code{n}, \code{power_pe}, \code{power_hdmm},
\code{n_valid}, and \code{n_empty} (see \code{\link[=pe_power_curve]{pe_power_curve()}}). The smallest \code{n}
reaching \code{target_power} for the PE test is stored in the
\code{"recommended_n"} attribute (\code{NA} if no grid value reaches it), with
\code{"target_power"}, \code{"outcome"}, \code{"pattern"}, \code{"c1"}, \code{"c2"}, \code{"n_rep"},
and \code{"alpha_level"}.
}
\description{
Estimates, by Monte Carlo simulation, the sample size needed for the
power-enhanced test to reach a target power under a given mediation
pattern and signal strength. It evaluates the empirical power of the PE
test (and, for comparison, the benchmark Wald test) at each candidate
sample size on a grid, and reports the smallest grid value that reaches
the target. This is the design counterpart of the analysis function
\code{\link[=pe_mediation]{pe_mediation()}}: use it to plan a study, then \code{\link[=pe_mediation]{pe_mediation()}} to
analyze it.
}
\details{
This is a simulation-based procedure, not a closed-form power formula:
no analytic power expression exists for the PE test, so sample size is
instead determined by direct Monte Carlo simulation. Power is estimated
by simulating \code{n_rep} data sets at each candidate
\code{n} with \code{\link[=simulate_mediation_data]{simulate_mediation_data()}} and recording how often the test
rejects at level \code{alpha_level}. Because the estimate is a Monte Carlo
proportion, it carries simulation error of roughly
\eqn{\sqrt{power(1 - power) / n\_rep}}; raise \code{n_rep} for a smoother
curve and a more stable recommendation. The grid approach (rather than
a root search) is deliberate: the power curve is monotone but noisy, so
a search can stop early on a lucky draw, whereas the grid shows the
whole trajectory.
}
\examples{
# n_rep = 5 keeps this example fast; a power estimate from five
# replications has a Monte Carlo standard error of up to 0.22, so the
# recommended n here is a demonstration, not a plan. Planning a study
# deserves n_rep of several hundred.
set.seed(113)
plan <- ss_power_pe_mediation(outcome = "continuous", pattern = "contrasting",
                              p = 50, n_grid = c(80, 120, 160), n_rep = 5)
plan
attr(plan, "recommended_n")

}
\seealso{
\code{\link[=pe_power_curve]{pe_power_curve()}}, \code{\link[=pe_mediation]{pe_mediation()}},
\code{\link[=simulate_mediation_data]{simulate_mediation_data()}}.

Other mediation simulation:
\code{\link{guo_calibration}},
\code{\link[=mediation_ar1_cov]{mediation_ar1_cov()}},
\code{\link[=pe_identification_study]{pe_identification_study()}},
\code{\link[=pe_power_curve]{pe_power_curve()}},
\code{\link[=pe_simulation_study]{pe_simulation_study()}},
\code{\link[=plot.poem_tbl]{plot.poem_tbl()}},
\code{\link[=simulate_guo_mediation]{simulate_guo_mediation()}},
\code{\link[=simulate_mediation_data]{simulate_mediation_data()}}
}
\author{
Xiufan Yu and Ken Kelley
}
\concept{mediation simulation}
