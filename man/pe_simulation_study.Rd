% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pe_simulation_study.R
\name{pe_simulation_study}
\alias{pe_simulation_study}
\title{Run the power study under several mediation patterns at once}
\usage{
pe_simulation_study(
  n,
  p,
  outcome = c("continuous", "binary", "count"),
  patterns = c("homogeneous", "contrasting"),
  c1_grid = seq(0, 1, by = 0.25),
  c2 = 0.5,
  n_rep = 100,
  alpha_level = 0.05,
  method = c("Bonferroni", "BH", "BY"),
  error_level = 0.05,
  lambda_grid = NULL,
  lambda_grid_reduced = NULL,
  cores = 1L,
  seed = NULL
)
}
\arguments{
\item{n, p}{Observations and candidate mediators per simulated data set.}

\item{outcome}{Outcome type: \code{"continuous"}, \code{"binary"}, or \code{"count"}.}

\item{patterns}{Character vector of mediation patterns to run. Default
\code{c("homogeneous", "contrasting")}.}

\item{c1_grid, c2, n_rep, alpha_level, method, error_level, lambda_grid, lambda_grid_reduced, cores, seed}{Passed to \code{\link[=pe_power_curve]{pe_power_curve()}}.}
}
\value{
A tidy \code{data.frame} (class \code{poem_tbl}) with a leading \code{pattern}
column and the \code{\link[=pe_power_curve]{pe_power_curve()}} columns (\code{c1}, \code{rejection_hdmm},
\code{rejection_pe}, \code{n_valid}, \code{n_empty}) for each pattern. The settings
are recorded in the attributes \code{outcome}, \code{n}, \code{p}, \code{n_rep}, \code{alpha_level},
\code{error_level}, and \code{lambda_grid}.
}
\description{
A convenience wrapper around \code{\link[=pe_power_curve]{pe_power_curve()}} that runs the size and
power study under more than one mediation pattern in a single call and
stacks the results, so a homogeneous and a contrasting curve (the two
panels of a figure in the article come back
together. Each pattern is swept over the same signal grid.
}
\examples{
# n_rep = 5 keeps this example fast; a rate from five replications has a
# Monte Carlo standard error of up to 0.22, so read the shape, not the
# numbers. A reported study uses the article's design (n = 300, p = 500)
# and 1000 replications.
set.seed(113)
pe_simulation_study(n = 120, p = 50, outcome = "continuous",
                    c1_grid = c(0, 1), n_rep = 5)

}
\seealso{
\code{\link[=pe_power_curve]{pe_power_curve()}} for a single pattern, \code{\link[=plot.poem_tbl]{plot.poem_tbl()}} to
draw one pattern's curves.

Other mediation simulation:
\code{\link{guo_calibration}},
\code{\link[=mediation_ar1_cov]{mediation_ar1_cov()}},
\code{\link[=pe_identification_study]{pe_identification_study()}},
\code{\link[=pe_power_curve]{pe_power_curve()}},
\code{\link[=plot.poem_tbl]{plot.poem_tbl()}},
\code{\link[=simulate_guo_mediation]{simulate_guo_mediation()}},
\code{\link[=simulate_mediation_data]{simulate_mediation_data()}},
\code{\link[=ss_power_pe_mediation]{ss_power_pe_mediation()}}
}
\author{
Xiufan Yu and Ken Kelley
}
\concept{mediation simulation}
