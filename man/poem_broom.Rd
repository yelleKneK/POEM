% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_poem.R
\name{poem_broom}
\alias{poem_broom}
\alias{tidy.poem_tbl}
\alias{glance.poem_tbl}
\alias{tidy}
\alias{glance}
\title{Broom verbs for POEM results}
\usage{
\method{tidy}{poem_tbl}(x, ...)

\method{glance}{poem_tbl}(x, ...)

tidy(x, ...)

glance(x, ...)
}
\arguments{
\item{x}{A \code{poem_tbl} result.}

\item{...}{Unused, for generic compatibility.}
}
\value{
A \code{data.frame}. For a fit, \code{tidy()} has the columns of
\code{\link[=pe_mediators]{pe_mediators()}} and \code{glance()} the columns \code{stat_hdmm}, \code{pval_hdmm},
\code{stat_pe}, \code{pval_pe}, \code{total_indirect_effect} (one column per
exposure when there are several, suffixed with the exposure's column
name or, for an unnamed \code{X}, \verb{_1}, \verb{_2}, ...), \code{n_active_mediators},
and \code{n_observations}.
}
\description{
\code{tidy()} and \code{glance()} methods so a POEM result composes with the
broom ecosystem; the generics are re-exported, so \code{tidy(fit)} and
\code{glance(fit)} work after \code{library(POEM)} alone. For a \code{\link[=pe_mediation]{pe_mediation()}}
fit, \code{tidy()} returns the per-mediator table (see \code{\link[=pe_mediators]{pe_mediators()}}) and
\code{glance()} returns a one-row summary of the two tests and the total
indirect effect. For a \code{\link[=pe_power_curve]{pe_power_curve()}} table both return the table
itself (it is already one row per grid point).
}
\examples{
set.seed(113)
d <- simulate_mediation_data(n = 120, p = 40, pattern = "contrasting",
                             outcome = "continuous", c1 = 1)
fit <- pe_mediation(d$X, d$Y, d$M, outcome = "continuous")
tidy(fit)
glance(fit)

}
\seealso{
\code{\link[=pe_mediation]{pe_mediation()}}, \code{\link[=pe_mediators]{pe_mediators()}}.
}
\author{
Xiufan Yu and Ken Kelley
}
