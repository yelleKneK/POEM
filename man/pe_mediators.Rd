% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pe_accessors.R
\name{pe_mediators}
\alias{pe_mediators}
\title{Per-mediator detail behind a power-enhanced mediation test}
\usage{
pe_mediators(fit)
}
\arguments{
\item{fit}{A result from \code{\link[=pe_mediation]{pe_mediation()}} or one of its workers.}
}
\value{
A tidy \code{data.frame} (class \code{poem_tbl}) with columns:
\describe{
\item{mediator}{Column position of the mediator in the original \code{M}.}
\item{name}{The mediator's column name in \code{M}; present only when \code{M}
has column names.}
\item{t_outcome}{The standardized mediator-on-outcome statistic
(the \eqn{M \to Y} path, \eqn{\hat\alpha_{m,j} /
    \hat\sigma_{m,j}}).}
\item{t_exposure}{The strongest standardized exposure-on-mediator
statistic across exposures (the \eqn{X \to M} path).}
\item{screen_p}{The screening p-value, the larger of the two path
p-values (the smaller across exposures when there are several).
A mediator is active when this clears the multiplicity threshold.}
\item{selected}{Logical; whether the mediator was identified as
active.}
}
The table is empty when the fit selected no candidate mediators.
}
\description{
Returns the per-mediator table that explains a \code{\link[=pe_mediation]{pe_mediation()}} result:
one row for each mediator the penalized fit selected as a candidate,
with its two path statistics, the screening p-value, and whether it
was identified as active. This is the "why" behind the active set, so a
user can see which mediators were close to the screening threshold and
which were screened out.
}
\examples{
set.seed(113)
d <- simulate_mediation_data(n = 200, p = 60, outcome = "continuous",
                             pattern = "contrasting", c1 = 1)
fit <- pe_mediation(d$X, d$Y, d$M, outcome = "continuous")
pe_mediators(fit)

}
\seealso{
\code{\link[=pe_mediation]{pe_mediation()}}, \code{\link[=pe_selection]{pe_selection()}}.

Other mediation tests:
\code{\link[=pe_lambda_grid]{pe_lambda_grid()}},
\code{\link[=pe_mediate]{pe_mediate()}},
\code{\link[=pe_mediation]{pe_mediation()}},
\code{\link[=pe_mediation_linear]{pe_mediation_linear()}},
\code{\link[=pe_mediation_logistic]{pe_mediation_logistic()}},
\code{\link[=pe_mediation_poisson]{pe_mediation_poisson()}},
\code{\link[=pe_selection]{pe_selection()}},
\code{\link[=summary.poem_tbl]{summary.poem_tbl()}}
}
\author{
Xiufan Yu and Ken Kelley
}
\concept{mediation tests}
