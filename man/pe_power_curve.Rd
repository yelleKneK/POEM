% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pe_power_curve.R
\name{pe_power_curve}
\alias{pe_power_curve}
\title{Monte Carlo size and power curve for the PE mediation tests}
\usage{
pe_power_curve(
  n,
  p,
  outcome = c("continuous", "binary", "count"),
  pattern = c("homogeneous", "contrasting", "heterogeneous"),
  c1_grid = seq(0, 1, by = 0.25),
  c2 = 0.5,
  n_rep = 100,
  alpha_level = 0.05,
  method = c("Bonferroni", "BH", "BY"),
  error_level = 0.05,
  lambda_grid = NULL,
  lambda_grid_reduced = NULL,
  outcome_args = list(),
  cores = 1L,
  seed = NULL,
  progress = FALSE
)
}
\arguments{
\item{n, p}{Number of observations and candidate mediators per data set.}

\item{outcome}{Outcome type passed to \code{\link[=simulate_mediation_data]{simulate_mediation_data()}}:
\code{"continuous"}, \code{"binary"}, or \code{"count"}.}

\item{pattern}{Mediation pattern: \code{"homogeneous"} or \code{"contrasting"}
(\code{"heterogeneous"} is a synonym for \code{"contrasting"}).}

\item{c1_grid}{Numeric vector of signal-strength scales to sweep.
Default \code{seq(0, 1, by = 0.25)}. Include 0 to estimate the Type I
error rate; the article also uses negative values.}

\item{c2}{Direct effect used in the simulation. Default 0.5.}

\item{n_rep}{Number of Monte Carlo replications per grid point. Default
100. The article uses 1000.}

\item{alpha_level}{Significance level for the global test. A replication
counts as a rejection when its p-value is at most \code{alpha_level}. Default
0.05.}

\item{method, error_level}{Passed to \code{\link[=pe_mediation]{pe_mediation()}}: the multiplicity
method and target error rate for the mediator-screening step.}

\item{lambda_grid, lambda_grid_reduced}{Passed to \code{\link[=pe_mediation]{pe_mediation()}}: the
tuning grids for the penalized fit. Default \code{NULL}, the family's
default grid (see \code{\link[=pe_lambda_grid]{pe_lambda_grid()}}).}

\item{outcome_args}{A list of further arguments forwarded to
\code{\link[=simulate_mediation_data]{simulate_mediation_data()}} (for example \code{rho}, \code{q}, \code{d}, \code{tau},
\code{alpha_m}). Default empty. The design arguments this function sets
itself (\code{n}, \code{p}, \code{outcome}, \code{pattern}, \code{c1}, \code{c2}) and \code{seed} may not
appear here; a \code{seed} inside \code{outcome_args} would make every
replication draw the same data.}

\item{cores}{Number of CPU cores for the Monte Carlo replications.
Values above 1 fork via the base \pkg{parallel} package (Unix only).
With parallel cores, reproducibility uses parallel-safe streams, so a
seeded parallel run is reproducible across runs at the same \code{cores} but
need not match a serial run. Default 1.}

\item{seed}{Optional integer seed, set locally with the caller's random
number generator state restored on exit.}

\item{progress}{Logical; if \code{TRUE}, print a line per grid point as it
completes. Default \code{FALSE}.}
}
\value{
A tidy \code{data.frame} of class \code{poem_tbl} with one row per grid
point and columns \code{c1}, \code{rejection_hdmm}, and \code{rejection_pe} (the
empirical rejection rates of the benchmark and power-enhanced tests),
\code{n_valid} (replications whose fit succeeded, the denominator of the
rates), and \code{n_empty} (replications whose penalized fit selected no
mediator, counted as non-rejections). The simulation settings are
recorded in the attributes \code{outcome}, \code{pattern}, \code{n}, \code{p}, \code{n_rep},
\code{alpha_level}, \code{error_level}, and \code{lambda_grid}.
}
\description{
Reproduces the article's simulation studies: for each value of the
signal-strength scale \eqn{c_1} on a grid, it simulates many data sets,
applies both the benchmark Wald test and the power-enhanced test, and
returns their empirical rejection rates. At \eqn{c_1 = 0} (the global
null of no mediation) the rejection rate estimates the Type I error
rate; at nonzero \eqn{c_1} it estimates power. The headline finding is
visible directly in the table: under a contrasting pattern the
benchmark test stays near its nominal size as \eqn{|c_1|} grows while
the power-enhanced test climbs toward one.
}
\details{
A rejection rate from \code{n_rep} replications carries a Monte Carlo
standard error of about \eqn{\sqrt{r(1 - r) / n_{rep}}}; at 100
replications a rate near 0.05 is known to about 0.02 and a rate near
0.5 to about 0.05. The article uses 1000 replications. A replication
whose penalized fit selects no mediator counts as a non-rejection (both
p-values are 1), the article's convention; the number of such
replications is reported in \code{n_empty}. A replication whose fit fails
outright is dropped from the denominator and reported in \code{n_valid}, with
a warning that quotes the first error.

The tuning grid matters for what these curves show. The article's
figures were produced with per-setting grids (see \code{\link[=pe_lambda_grid]{pe_lambda_grid()}});
the default here is that grid rescaled to \code{n} and \code{p}, so the
article's settings reproduce its curves within Monte Carlo error.
Pass \code{lambda_grid} to study another grid.
}
\examples{
# n_rep = 5 keeps this example fast; a rate from five replications has a
# Monte Carlo standard error of up to 0.22, so read the shape, not the
# numbers. A reported study uses the article's design (n = 300, p = 500)
# and 1000 replications.
set.seed(113)
pe_power_curve(n = 200, p = 60, outcome = "continuous",
               pattern = "contrasting", c1_grid = c(0, 0.5, 1),
               n_rep = 5)

}
\seealso{
\code{\link[=pe_mediation]{pe_mediation()}}, \code{\link[=simulate_mediation_data]{simulate_mediation_data()}},
\code{\link[=pe_lambda_grid]{pe_lambda_grid()}}.

Other mediation simulation:
\code{\link{guo_calibration}},
\code{\link[=mediation_ar1_cov]{mediation_ar1_cov()}},
\code{\link[=pe_identification_study]{pe_identification_study()}},
\code{\link[=pe_simulation_study]{pe_simulation_study()}},
\code{\link[=plot.poem_tbl]{plot.poem_tbl()}},
\code{\link[=simulate_guo_mediation]{simulate_guo_mediation()}},
\code{\link[=simulate_mediation_data]{simulate_mediation_data()}},
\code{\link[=ss_power_pe_mediation]{ss_power_pe_mediation()}}
}
\author{
Xiufan Yu and Ken Kelley
}
\concept{mediation simulation}
