% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pe_accessors.R
\name{pe_selection}
\alias{pe_selection}
\title{Active mediators identified under each multiplicity method}
\usage{
pe_selection(fit)
}
\arguments{
\item{fit}{A result from \code{\link[=pe_mediation]{pe_mediation()}} or one of its workers.}
}
\value{
A tidy \code{data.frame} of class \code{poem_tbl} (so its p-values print
to fixed decimals) with one row per multiplicity method and columns
\code{method}, \code{n_active} (number of active mediators), \code{j_pe} (the power
enhancement component \eqn{J_m} under that method's screen), \code{stat_pe}
and \code{pval_pe} (the resulting power-enhanced statistic \eqn{M_{PE}} and
its p-value), and \code{active_mediators} (their column names when \code{M} has
them, otherwise their column positions, comma-separated, or
\code{"none"}). Because each multiplicity method screens
a different active set into \eqn{J_m}, the \code{pval_pe} column gives the
PE / PE_BH / PE_BY global p-values side by side, as the article's
extended data-analysis tables report them.
}
\description{
Summarizes which mediators a \code{\link[=pe_mediation]{pe_mediation()}} fit identifies as active.
When the fit was produced with \code{report_all_methods = TRUE}, this
reports the active set under each of the three multiplicity methods
(Bonferroni for familywise error rate control, Benjamini-Hochberg and
Benjamini-Yekutieli for false discovery rate control), so their
conservativeness can be compared on the same fit. Otherwise it reports
the single method that was used.
}
\examples{
set.seed(113)
d <- simulate_mediation_data(n = 200, p = 60, outcome = "continuous",
                             pattern = "contrasting", c1 = 1)
fit <- pe_mediation(d$X, d$Y, d$M, outcome = "continuous",
                    report_all_methods = TRUE)
pe_selection(fit)

}
\seealso{
\code{\link[=pe_mediation]{pe_mediation()}} (its \code{report_all_methods} argument),
\code{\link[=pe_mediators]{pe_mediators()}}.

Other mediation tests:
\code{\link[=pe_lambda_grid]{pe_lambda_grid()}},
\code{\link[=pe_mediate]{pe_mediate()}},
\code{\link[=pe_mediation]{pe_mediation()}},
\code{\link[=pe_mediation_linear]{pe_mediation_linear()}},
\code{\link[=pe_mediation_logistic]{pe_mediation_logistic()}},
\code{\link[=pe_mediation_poisson]{pe_mediation_poisson()}},
\code{\link[=pe_mediators]{pe_mediators()}},
\code{\link[=summary.poem_tbl]{summary.poem_tbl()}}
}
\author{
Xiufan Yu and Ken Kelley
}
\concept{mediation tests}
