% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pe_identification_study.R
\name{pe_identification_study}
\alias{pe_identification_study}
\title{Monte Carlo study of individual-mediator identification (FWER / FDR)}
\usage{
pe_identification_study(
  n,
  p,
  outcome = c("continuous", "binary", "count"),
  pattern = c("homogeneous", "contrasting", "heterogeneous"),
  c1_grid = seq(0, 1, by = 0.25),
  c2 = 0.5,
  n_rep = 200,
  methods = c("Bonferroni", "BH", "BY"),
  error_level = 0.05,
  truth = c("outcome_effect", "mediation"),
  lambda_grid = NULL,
  lambda_grid_reduced = NULL,
  outcome_args = list(),
  cores = 1L,
  seed = NULL,
  progress = FALSE
)
}
\arguments{
\item{n, p}{Number of observations and candidate mediators per data set.}

\item{outcome}{Outcome type passed to \code{\link[=simulate_mediation_data]{simulate_mediation_data()}}:
\code{"continuous"}, \code{"binary"}, or \code{"count"}.}

\item{pattern}{Mediation pattern: \code{"homogeneous"} or \code{"contrasting"}
(\code{"heterogeneous"} is a synonym for \code{"contrasting"}).}

\item{c1_grid}{Numeric vector of signal-strength scales to sweep.
Default \code{seq(0, 1, by = 0.25)}. Include 0 to estimate the null
familywise error rate.}

\item{c2}{Direct effect used in the simulation. Default 0.5.}

\item{n_rep}{Number of Monte Carlo replications per grid point. Default
200. The article uses 1000.}

\item{methods}{Multiplicity methods to evaluate, any of \code{"Bonferroni"}
(familywise error rate), \code{"BH"}, and \code{"BY"} (false discovery rate).
Default all three.}

\item{error_level}{Target error rate for the mediator-screening step
(the FWER level for Bonferroni, the FDR level for BH and BY). Default
0.05.}

\item{truth}{Which mediators count as truly active: \code{"outcome_effect"}
(nonzero outcome coefficient, the article's convention) or
\code{"mediation"} (both paths nonzero). See Details.}

\item{lambda_grid, lambda_grid_reduced}{Passed to \code{\link[=pe_mediation]{pe_mediation()}}: the
tuning grids for the penalized fit. Default \code{NULL}, the family's
default grid (see \code{\link[=pe_lambda_grid]{pe_lambda_grid()}}).}

\item{outcome_args}{A list of further arguments forwarded to
\code{\link[=simulate_mediation_data]{simulate_mediation_data()}} (for example \code{rho}, \code{q}, \code{d}, \code{tau},
\code{alpha_m}). Default empty. The design arguments this function sets
itself (\code{n}, \code{p}, \code{outcome}, \code{pattern}, \code{c1}, \code{c2}) and \code{seed} may not
appear here.}

\item{cores}{Number of CPU cores for the replications. Values above 1
fork via the base \pkg{parallel} package (Unix only); a seeded parallel
run is reproducible across runs at the same \code{cores} but need not match a
serial run. Default 1.}

\item{seed}{Optional integer seed, set locally with the caller's random
number generator state restored on exit.}

\item{progress}{Logical; if \code{TRUE}, print a line per grid point. Default
\code{FALSE}.}
}
\value{
A tidy \code{data.frame} of class \code{poem_tbl} with one row per
(\eqn{c_1}, method) combination and columns \code{c1}, \code{method}, \code{fwer},
\code{fdr}, \code{precision}, \code{recall} (as defined in Details), \code{n_valid}
(replications whose fit succeeded), and \code{n_empty} (replications whose
penalized fit selected no mediator). The settings are recorded in the
attributes \code{outcome}, \code{pattern}, \code{n}, \code{p}, \code{n_rep}, \code{error_level},
\code{truth}, and \code{lambda_grid}.
}
\description{
Evaluates how well the power-enhanced screen recovers the \emph{individual}
active mediators, the experiment reported in the article's supplement.
For each signal-strength scale \eqn{c_1} on a grid it simulates many data
sets, identifies the active set under each multiplicity method, and scores
those selections against the known truth, returning the empirical
familywise error rate, false discovery rate, precision, and recall. This
complements \code{\link[=pe_power_curve]{pe_power_curve()}}, which evaluates the \emph{global} test rather
than which mediators are flagged.
}
\details{
The four metrics follow the article's supplement. In each replication
the selected set is compared with the truth set; the familywise error
rate is the proportion of replications selecting at least one mediator
outside the truth set, the false discovery rate is the mean false
discovery proportion (zero when nothing is selected), precision is the
mean proportion of selected mediators that are true (scored zero when
nothing is selected), and recall is the mean proportion of true mediators
selected. Each is a Monte Carlo proportion with standard error about
\eqn{\sqrt{r(1 - r) / n_{rep}}}; the article uses 1000 replications.

What counts as a true mediator is set by \code{truth}. The article scores a
mediator as active when its outcome coefficient \eqn{\alpha_{m,j}} is
nonzero (\code{truth = "outcome_effect"}, the default), which is what its
supplement tables use and what makes precision and recall defined at
\eqn{c_1 = 0}, where the exposure-on-mediator paths are all zero; there
the familywise error rate is the probability of selecting a mediator
with no outcome effect. Under \code{truth = "mediation"} a mediator is active
only when both paths are nonzero (the simulator's \code{active_mediators}), so
at \eqn{c_1 = 0} no mediator is active, any selection is a false
positive, and recall is \code{NA}. For nonzero \eqn{c_1} the two definitions
agree under the article's designs, whose exposure-on-mediator
loadings are all nonzero.

The tuning grid governs these rates as much as the screen does (see
\code{\link[=pe_lambda_grid]{pe_lambda_grid()}}); the default reproduces the article's settings.
}
\examples{
# n_rep = 5 keeps this example fast; a rate from five replications has a
# Monte Carlo standard error of up to 0.22, so read the shape, not the
# numbers. A reported study uses the article's design (n = 300, p = 500)
# and 1000 replications.
set.seed(113)
pe_identification_study(n = 200, p = 60, outcome = "continuous",
                        pattern = "contrasting", c1_grid = c(0, 1),
                        n_rep = 5)

}
\references{
Yu, X., and Kelley, K. (in press). Power Enhancement in
High-Dimensional Heterogeneous Mediation Analysis. \emph{Journal of the
American Statistical Association}.
}
\seealso{
\code{\link[=pe_power_curve]{pe_power_curve()}} for the global test, \code{\link[=pe_selection]{pe_selection()}} for the
per-method active set of a single fit, \code{\link[=pe_lambda_grid]{pe_lambda_grid()}}.

Other mediation simulation:
\code{\link{guo_calibration}},
\code{\link[=mediation_ar1_cov]{mediation_ar1_cov()}},
\code{\link[=pe_power_curve]{pe_power_curve()}},
\code{\link[=pe_simulation_study]{pe_simulation_study()}},
\code{\link[=plot.poem_tbl]{plot.poem_tbl()}},
\code{\link[=simulate_guo_mediation]{simulate_guo_mediation()}},
\code{\link[=simulate_mediation_data]{simulate_mediation_data()}},
\code{\link[=ss_power_pe_mediation]{ss_power_pe_mediation()}}
}
\author{
Xiufan Yu and Ken Kelley
}
\concept{mediation simulation}
