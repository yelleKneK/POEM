% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pe_mediation_linear.R
\name{pe_mediation_linear}
\alias{pe_mediation_linear}
\title{Power-enhanced mediation test for a continuous outcome}
\usage{
pe_mediation_linear(
  X,
  Y,
  M,
  Z = NULL,
  method = c("Bonferroni", "BH", "BY"),
  scale = TRUE,
  error_level = 0.05,
  conf_level = 0.95,
  report_all_methods = FALSE,
  drop_constant = FALSE,
  lambda_grid = NULL,
  lambda_grid_reduced = NULL
)
}
\arguments{
\item{X}{Numeric matrix of exposures with \eqn{n} rows and \eqn{q}
columns (\eqn{q} is usually 1). A numeric vector is treated as a
single exposure.}

\item{Y}{Numeric outcome vector of length \eqn{n}. Continuous for
\code{outcome = "continuous"}, coded 0/1 for \code{"binary"}, and nonnegative
integer counts for \code{"count"}.}

\item{M}{Numeric matrix of candidate mediators with \eqn{n} rows and
\eqn{p} columns; \eqn{p} may exceed \eqn{n}. Column names, when
present, label the active mediators in the print footer and the
mediator table; without them mediators are reported by column
position.}

\item{Z}{Optional numeric matrix of confounders with \eqn{n} rows.
Default \code{NULL} (no confounders).}

\item{method}{Multiplicity adjustment used to screen individual
mediators in the PE component: \code{"Bonferroni"} (familywise error
rate control, the default), \code{"BH"}, or \code{"BY"} (false discovery rate
control; \code{"BH"} assumes independence or positive dependence among
mediators, \code{"BY"} is valid under arbitrary dependence).}

\item{scale}{Logical; if \code{TRUE} (default) the exposures, mediators,
and confounders are standardized (and a continuous outcome is
centered) before fitting, as the method assumes.}

\item{error_level}{Target error rate for the mediator-screening step,
in \eqn{(0, 1)}. Interpreted as the familywise error rate when
\code{method = "Bonferroni"} and as the false discovery rate when
\code{method = "BH"} or \code{"BY"}. Default 0.05. This is distinct from the
significance level used to test the global null (which is the user's
choice when reading \code{pval_pe}); the test is insensitive to
\code{error_level} and remains valid for any fixed value in \eqn{(0, 1)}.}

\item{conf_level}{Confidence level for the interval on the total
indirect effect. Default 0.95.}

\item{report_all_methods}{Logical; if \code{TRUE}, the active-mediator set is
computed for all three multiplicity methods (Bonferroni, BH, BY) off the
same fit and recorded for comparison, retrievable with \code{\link[=pe_selection]{pe_selection()}}.
The \code{method} argument still drives the primary result. Default \code{FALSE}.}

\item{drop_constant}{Logical; how to handle constant (zero-variance)
mediator columns, which cannot be standardized or carry signal. If
\code{FALSE} (the default) they are an error; if \code{TRUE} they are dropped
with a warning and the remaining mediators are renumbered back to their
original column positions in the reported active set. Useful for small
subgroups where some mediators happen to be constant.}

\item{lambda_grid}{Numeric vector of candidate SCAD tuning parameters
for the penalized mediator fit; the fit is repeated at each value and
the one minimizing the high-dimensional BIC (HBIC) is kept. Default
\code{NULL} uses, for a continuous outcome, \code{\link[=pe_lambda_grid]{pe_lambda_grid()}}: the grid
the article's simulations used at their design, rescaled to this \code{n}
and \code{p} by the rate \eqn{\sqrt{\log p / n}} the theory requires of the
tuning parameter; for a binary or count outcome it uses the review-era
grid \code{seq(0.05, 1, length.out = 20)}. The grid's lower end matters
most, because the HBIC minimum often sits there and because it decides
whether the identified set keeps its error-rate guarantee; see
\code{\link[=pe_lambda_grid]{pe_lambda_grid()}} for the trade-off with power, measured. The value
chosen is reported in the \code{"tuning"} attribute and the print footer.}

\item{lambda_grid_reduced}{Numeric vector of candidate tuning
parameters for the reduced-model fit that the continuous-outcome Wald
test refits (unused for binary and count outcomes). Default \code{NULL}
uses \code{\link[=pe_lambda_grid]{pe_lambda_grid()}} with \code{model = "reduced"} when \code{lambda_grid} is
also \code{NULL} (the article's reduced-model grid, rescaled), and
otherwise \code{lambda_grid} itself.}
}
\value{
A tidy \code{data.frame} of class \code{poem_tbl} with rows
\code{stat_hdmm} and \code{pval_hdmm} (the benchmark Wald test),
\code{stat_pe}, \code{j_pe}, and \code{pval_pe} (the power-enhanced test and its
PE component), \code{total_indirect_effect} with \code{total_indirect_lower}
and \code{total_indirect_upper} (the estimate and a Wald interval at
\code{conf_level}, the estimate plus or minus a standard normal quantile
times its standard error; one row of each per exposure when \code{q > 1},
suffixed with the exposure's column name when \code{X} has column names
and with \verb{_1}, \verb{_2}, ... otherwise), \code{n_active_mediators}, \code{df} (the
chi-square degrees of freedom, equal to \code{q}), \code{n_candidate_mediators},
and \code{n_observations}. Attributes: \code{"active_mediators"} (the column
positions in \code{M} identified as active; the print footer shows their
column names when \code{M} has them), \code{"mediator_names"} and
\code{"exposure_names"} (the column names of \code{M} and \code{X}, or \code{NULL} when
unnamed), \code{"mediator_table"} (the per-mediator screening statistics,
see \code{\link[=pe_mediators]{pe_mediators()}}), \code{"method"}, \code{"error_level"}, \code{"conf_level"},
\code{"outcome"}, \code{"tuning"} (a list with the grid searched, the
HBIC-selected \code{lambda_selected}, and \code{at_lower_end}, \code{TRUE} when the
selection sat at the grid's smallest value; for a continuous outcome
also the reduced model's grid and selection), \code{"empty_fit"}, and,
with \code{report_all_methods = TRUE}, \code{"selection_by_method"} and
\code{"pe_by_method"} (see \code{\link[=pe_selection]{pe_selection()}}).

When the penalized fit selects no mediator at any grid value there is
nothing to test: the function warns (a condition of class
\code{poem_empty_fit}), sets \code{"empty_fit"} to \code{TRUE}, and returns the same
rows with both statistics 0, both p-values 1 (the article's Monte
Carlo convention, under which an empty selection is a non-rejection),
a total indirect effect of 0, and \code{NA} interval limits. A p-value of 1
from such a fit is not evidence for the null; a grid reaching smaller
values may select mediators.
}
\description{
Tests the global null hypothesis of no active mediator among a
high-dimensional set of candidate mediators, for a continuous
(linear-model) outcome, and reports both the benchmark Wald test on
the total indirect effect and the power-enhanced (PE) test that
remains powerful when individual mediation effects are heterogeneous
or contrasting. This is the continuous-outcome worker behind
\code{\link[=pe_mediation]{pe_mediation()}}; call it directly when the outcome is continuous.
}
\details{
The model is the linear mediation pair
\deqn{Y = \alpha_m' M + \alpha_x' X + \alpha_z' Z + \varepsilon_y,
      \qquad M = \Gamma_x' X + \Gamma_z' Z + \varepsilon_m,}
with total indirect effect \eqn{\beta = \Gamma_x \alpha_m}. The
mediator coefficients \eqn{\alpha_m} are estimated by partial
penalized least squares with a SCAD penalty, the tuning parameter
chosen by the high-dimensional BIC. The benchmark statistic is the
Wald statistic \eqn{S_n = n \hat\beta' \hat\Sigma_\beta^{-1}
\hat\beta} of Guo et al. (2022); the power-enhanced statistic adds the
component \eqn{J_m} that accumulates the marginal signal from each
selected mediator (see \code{\link[=pe_mediation]{pe_mediation()}} for the rationale and the
formula). Both are referred to a chi-square distribution with \eqn{q}
(the number of exposures) degrees of freedom under the global null.
}
\examples{
set.seed(113)
# A contrasting setting: two active mediators whose indirect effects
# cancel, so the total indirect effect is zero. The Wald test is
# near powerless here; the PE test rejects and identifies active
# mediators.
d <- simulate_mediation_data(n = 200, p = 60, pattern = "contrasting",
                             outcome = "continuous", c1 = 1)
pe_mediation_linear(d$X, d$Y, d$M)

}
\references{
Guo, X., Li, R., Liu, J., and Zeng, M. (2022). High-dimensional
mediation analysis for selecting DNA methylation loci mediating
childhood trauma and cortisol stress reactivity. \emph{Journal of the
American Statistical Association, 117}(539), 1110--1121.
\doi{10.1080/01621459.2022.2053136}

Fan, J., and Li, R. (2001). Variable selection via nonconcave penalized
likelihood and its oracle properties. \emph{Journal of the American
Statistical Association, 96}(456), 1348--1360.
\doi{10.1198/016214501753382273}

Wang, L., Kim, Y., and Li, R. (2013). Calibrating nonconvex penalized
regression in ultra-high dimension. \emph{The Annals of Statistics,
41}(5), 2505--2536. \doi{10.1214/13-AOS1159}
}
\seealso{
\code{\link[=pe_mediation]{pe_mediation()}} for the front end, \code{\link[=pe_mediation_logistic]{pe_mediation_logistic()}}
and \code{\link[=pe_mediation_poisson]{pe_mediation_poisson()}} for binary and count outcomes, and
\code{\link[=simulate_mediation_data]{simulate_mediation_data()}} to generate data for trying the method.

Other mediation tests:
\code{\link[=pe_lambda_grid]{pe_lambda_grid()}},
\code{\link[=pe_mediate]{pe_mediate()}},
\code{\link[=pe_mediation]{pe_mediation()}},
\code{\link[=pe_mediation_logistic]{pe_mediation_logistic()}},
\code{\link[=pe_mediation_poisson]{pe_mediation_poisson()}},
\code{\link[=pe_mediators]{pe_mediators()}},
\code{\link[=pe_selection]{pe_selection()}},
\code{\link[=summary.poem_tbl]{summary.poem_tbl()}}
}
\author{
Xiufan Yu and Ken Kelley
}
\concept{mediation tests}
